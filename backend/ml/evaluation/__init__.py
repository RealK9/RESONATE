"""
Evaluation framework for RESONATE's AI model pipeline.

Provides structured metrics for sample-level analysis (role classification,
pitch/key estimation, descriptor stability, embedding retrieval coherence),
mix-level analysis (source-role detection, need inference quality,
style classification accuracy), recommendation quality (precision@k,
acceptance rate, preference win rate, diversity), and system-level
performance (latency, throughput, query speed, fallback success).
"""
